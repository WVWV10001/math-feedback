<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>作者：王铁</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@1,400;1,600&family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <style>
      :root {
        --bg-color: #050505;
        --primary-blue: #00e5ff;
        --text-main: #ffffff;
        --text-dim: #888888;
        --grid-color: rgba(255, 255, 255, 0.03);
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        background-color: var(--bg-color);
        color: var(--text-main);
        font-family: "Inter", sans-serif;
        overflow-x: hidden;
      }

      .gsap-card,
      .gsap-stat,
      .gsap-fade-up,
      .hero-content,
      .hero-visual {
        will-change: transform, opacity;
      }

      /* ===== 固定背景层 ===== */
      #canvas-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -2;
      }

      .background-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image:
          linear-gradient(var(--grid-color) 1px, transparent 1px),
          linear-gradient(90deg, var(--grid-color) 1px, transparent 1px);
        background-size: 80px 80px;
        z-index: -1;
        pointer-events: none;
      }

      /* ===== 导航栏 ===== */
      header {
        position: fixed;
        top: 30px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 100;
        transition: all 0.3s ease;
      }

      .nav-pill {
        background: rgba(10, 10, 10, 0.6);
        backdrop-filter: blur(15px);
        -webkit-backdrop-filter: blur(15px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        padding: 8px 12px;
        border-radius: 50px;
        display: flex;
        align-items: center;
        gap: 25px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
      }

      .logo {
        display: flex;
        align-items: center;
        gap: 10px;
        font-weight: 600;
        padding-left: 10px;
      }
      .logo-icon {
        width: 24px;
        height: 24px;
        background: linear-gradient(135deg, #222, var(--primary-blue));
        border-radius: 50%;
        position: relative;
      }
      .logo-icon::after {
        content: "";
        position: absolute;
        top: 50%;
        left: 50%;
        width: 12px;
        height: 1px;
        background: white;
        transform: translate(-50%, -50%) rotate(-45deg);
      }

      nav a {
        color: var(--text-dim);
        text-decoration: none;
        font-size: 13px;
        transition: color 0.3s;
      }
      nav a:hover {
        color: var(--text-main);
      }

      /* ===== Hero Section ===== */
      .hero-section {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 100px;
        height: 100vh;
        display: flex;
        align-items: center;
        position: relative;
        z-index: 2;
        pointer-events: none;
      }
      .hero-section > * {
        pointer-events: auto;
      }
      .hero-content {
        width: 50%;
      }

      .status-tag {
        font-family: "JetBrains Mono", monospace;
        font-size: 11px;
        letter-spacing: 2px;
        color: var(--text-dim);
        text-transform: uppercase;
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 30px;
      }
      .status-dot {
        width: 6px;
        height: 6px;
        background-color: var(--primary-blue);
        border-radius: 50%;
        box-shadow: 0 0 10px var(--primary-blue);
      }

      h1 {
        font-family: "Cormorant Garamond", serif;
        font-size: 82px;
        line-height: 0.95;
        font-style: italic;
        font-weight: 400;
        margin-bottom: 30px;
      }
      h1 span {
        color: var(--primary-blue);
        text-shadow: 0 0 30px rgba(0, 229, 255, 0.3);
      }
      .description {
        color: var(--text-dim);
        font-size: 18px;
        line-height: 1.6;
        max-width: 480px;
        margin-bottom: 50px;
      }

      .btn-group {
        display: flex;
        gap: 20px;
      }
      .btn-primary {
        background: rgba(0, 229, 255, 0.1);
        border: 1px solid rgba(0, 229, 255, 0.3);
        color: white;
        padding: 16px 32px;
        border-radius: 50px;
        font-weight: 500;
        text-decoration: none;
        transition: all 0.3s;
        cursor: pointer;
        font-size: 16px;
        font-family: inherit;
      }
      .btn-primary:hover {
        border-color: var(--primary-blue);
        background: rgba(0, 229, 255, 0.2);
        box-shadow: 0 0 20px rgba(0, 229, 255, 0.2);
      }

      .btn-secondary {
        border: 1px solid rgba(255, 255, 255, 0.1);
        background: transparent;
        color: var(--text-dim);
        padding: 16px 32px;
        border-radius: 50px;
        text-decoration: none;
        transition: 0.3s;
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 16px;
      }
      .btn-secondary:hover {
        border-color: rgba(255, 255, 255, 0.3);
        color: white;
      }

      .hero-visual {
        width: 50%;
        height: 100%;
        position: relative;
      }
      .svg-container {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 800px;
        height: 600px;
      }
      .tech-label {
        position: absolute;
        font-family: "JetBrains Mono", monospace;
        font-size: 11px;
        color: var(--primary-blue);
        text-transform: uppercase;
        letter-spacing: 1px;
        border-bottom: 1px solid var(--primary-blue);
        padding-bottom: 4px;
      }

      @keyframes rotate {
        from {
          transform: translate(-50%, -50%) rotate(0deg);
        }
        to {
          transform: translate(-50%, -50%) rotate(360deg);
        }
      }
      .radar-scanner {
        position: absolute;
        top: 55%;
        left: 45%;
        width: 200px;
        height: 200px;
        border-radius: 50%;
        border-top: 2px solid var(--primary-blue);
        transform: translate(-50%, -50%);
        animation: rotate 4s linear infinite;
        opacity: 0.5;
      }

      /* ===== 页面滚动内容区 ===== */
      .content-wrapper {
        position: relative;
        z-index: 10;
      }
      .section {
        max-width: 1400px;
        margin: 0 auto;
        padding: 150px 100px;
      }
      .section-header {
        margin-bottom: 80px;
      }
      .section-subtitle {
        font-family: "JetBrains Mono", monospace;
        color: var(--primary-blue);
        font-size: 12px;
        letter-spacing: 2px;
        text-transform: uppercase;
        margin-bottom: 15px;
        display: block;
      }
      .section-title {
        font-family: "Cormorant Garamond", serif;
        font-size: 56px;
        font-style: italic;
        font-weight: 400;
        line-height: 1.1;
      }

      .features-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 30px;
      }
      .feature-card {
        background: rgba(15, 15, 15, 0.4);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.05);
        padding: 50px 40px;
        border-radius: 20px;
        transition:
          transform 0.4s cubic-bezier(0.165, 0.84, 0.44, 1),
          border-color 0.4s ease;
      }
      .feature-card:hover {
        transform: translateY(-10px) scale(1.02);
        border-color: rgba(0, 229, 255, 0.3);
        background: rgba(20, 20, 25, 0.6);
      }
      .feature-icon {
        font-size: 32px;
        margin-bottom: 25px;
        color: var(--primary-blue);
      }
      .feature-title {
        font-size: 22px;
        font-weight: 500;
        margin-bottom: 15px;
      }
      .feature-desc {
        color: var(--text-dim);
        font-size: 15px;
        line-height: 1.6;
      }

      .stats-section {
        background: linear-gradient(to top, rgba(0, 0, 0, 0.8), transparent);
        border-top: 1px solid rgba(255, 255, 255, 0.05);
      }
      .stats-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 50px;
        text-align: center;
      }
      .stat-item h3 {
        font-family: "Cormorant Garamond", serif;
        font-size: 80px;
        font-weight: 400;
        color: var(--primary-blue);
        margin-bottom: 10px;
      }
      .stat-item p {
        font-family: "JetBrains Mono", monospace;
        color: var(--text-dim);
        font-size: 14px;
        text-transform: uppercase;
        letter-spacing: 1px;
      }

      footer {
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        padding: 50px 100px;
        text-align: center;
        background: #000;
      }
      footer p {
        color: var(--text-dim);
        font-size: 13px;
        font-family: "JetBrains Mono", monospace;
      }

      /* ===== 悬浮控制面板样式 ===== */
      .floating-btn {
        position: fixed;
        bottom: 40px;
        right: 40px;
        z-index: 200;
        pointer-events: auto;
      }
      .btn-toggle {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: linear-gradient(
          135deg,
          rgba(0, 229, 255, 0.2),
          rgba(0, 229, 255, 0.05)
        );
        border: 2px solid var(--primary-blue);
        color: var(--primary-blue);
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.3s ease;
        font-size: 24px;
        box-shadow: 0 0 20px rgba(0, 229, 255, 0.3);
        backdrop-filter: blur(10px);
      }
      .btn-toggle:hover {
        width: 70px;
        height: 70px;
        box-shadow: 0 0 40px rgba(0, 229, 255, 0.5);
      }
      .control-panel {
        position: fixed;
        bottom: 120px;
        right: 40px;
        width: 320px;
        background: rgba(10, 10, 10, 0.85);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(0, 229, 255, 0.2);
        border-radius: 20px;
        padding: 25px;
        box-shadow:
          0 20px 60px rgba(0, 0, 0, 0.7),
          0 0 40px rgba(0, 229, 255, 0.1);
        opacity: 0;
        visibility: hidden;
        transform: translateY(20px) scale(0.95);
        transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        z-index: 150;
        pointer-events: auto;
        max-height: 80vh;
        overflow-y: auto;
      }
      .control-panel.open {
        opacity: 1;
        visibility: visible;
        transform: translateY(0) scale(1);
      }
      .panel-title {
        font-size: 14px;
        font-weight: 600;
        color: var(--primary-blue);
        margin-bottom: 20px;
        text-transform: uppercase;
        letter-spacing: 2px;
        font-family: "JetBrains Mono", monospace;
      }
      .control-group {
        margin-bottom: 22px;
      }
      .control-label {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
        font-size: 12px;
        color: var(--text-dim);
        font-family: "JetBrains Mono", monospace;
      }
      .control-value {
        color: var(--primary-blue);
        font-weight: 600;
      }
      input[type="range"] {
        width: 100%;
        height: 4px;
        border-radius: 2px;
        background: linear-gradient(
          to right,
          rgba(0, 229, 255, 0.1) 0%,
          rgba(0, 229, 255, 0.3) 50%,
          rgba(0, 229, 255, 0.1) 100%
        );
        outline: none;
        -webkit-appearance: none;
        appearance: none;
        cursor: pointer;
      }
      input[type="range"]::-webkit-slider-thumb {
        -webkit-appearance: none;
        appearance: none;
        width: 16px;
        height: 16px;
        border-radius: 50%;
        background: var(--primary-blue);
        cursor: pointer;
        box-shadow: 0 0 15px rgba(0, 229, 255, 0.6);
        transition: all 0.2s;
      }
      input[type="range"]::-webkit-slider-thumb:hover {
        box-shadow: 0 0 25px rgba(0, 229, 255, 0.8);
        transform: scale(1.2);
      }
      .reset-btn {
        width: 100%;
        padding: 10px;
        border: 1px solid rgba(0, 229, 255, 0.2);
        background: transparent;
        color: var(--text-dim);
        border-radius: 10px;
        font-size: 12px;
        cursor: pointer;
        transition: all 0.3s;
        font-family: "JetBrains Mono", monospace;
        text-transform: uppercase;
        letter-spacing: 1px;
      }
      .reset-btn:hover {
        border-color: var(--primary-blue);
        color: var(--primary-blue);
        background: rgba(0, 229, 255, 0.05);
      }

      /* =========================================================
         新增：未来小红书宠物大博主王铁
         ========================================================= */
      .cyber-modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(5, 5, 5, 0.85);
        backdrop-filter: blur(15px);
        z-index: 9999;
        display: flex;
        justify-content: center;
        align-items: center;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.4s ease;
      }
      .cyber-modal-overlay.active {
        opacity: 1;
        pointer-events: auto;
      }
      .cyber-modal-content {
        background: rgba(15, 15, 15, 0.95);
        border: 1px solid rgba(0, 229, 255, 0.3);
        padding: 50px 40px;
        border-radius: 20px;
        text-align: center;
        max-width: 420px;
        width: 90%;
        transform: translateY(30px);
        transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        box-shadow:
          0 0 60px rgba(0, 229, 255, 0.15),
          inset 0 0 20px rgba(0, 229, 255, 0.05);
        position: relative;
      }
      .cyber-modal-overlay.active .cyber-modal-content {
        transform: translateY(0);
      }
      .cyber-close-btn {
        position: absolute;
        top: 15px;
        right: 20px;
        background: none;
        border: none;
        color: var(--text-dim);
        font-size: 32px;
        font-weight: 300;
        cursor: pointer;
        transition:
          color 0.3s,
          transform 0.3s;
      }
      .cyber-close-btn:hover {
        color: var(--primary-blue);
        transform: rotate(90deg);
      }
      .modal-title-neon {
        font-family: "JetBrains Mono", monospace;
        font-size: 12px;
        color: var(--primary-blue);
        margin-bottom: 30px;
        letter-spacing: 3px;
        display: inline-block;
        border-bottom: 1px dashed rgba(0, 229, 255, 0.3);
        padding-bottom: 5px;
      }
      .modal-body p {
        font-size: 15px;
        color: var(--text-dim);
        margin-bottom: 5px;
        letter-spacing: 1px;
      }
      .modal-body .highlight-text {
        font-size: 14px;
        color: white;
        font-weight: 600;
        text-shadow: 0 0 15px rgba(0, 229, 255, 0.6);
        margin-top: 15px;
        letter-spacing: 2px;
      }

      /* ===== 翻译切换按钮 ===== */
      .translate-toggle {
        position: fixed;
        bottom: 40px;
        left: 40px;
        z-index: 200;
        pointer-events: auto;
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .btn-translate {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: rgba(10, 10, 10, 0.75);
        border: 2px solid rgba(255, 255, 255, 0.2);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.3s ease;
        font-size: 13px;
        font-weight: 600;
        font-family: "JetBrains Mono", monospace;
        backdrop-filter: blur(10px);
        letter-spacing: 1px;
      }
      .btn-translate:hover {
        border-color: #00e5ff;
        box-shadow: 0 0 25px rgba(0, 229, 255, 0.3);
        transform: scale(1.08);
      }
      .btn-translate.active-lang {
        border-color: #00e5ff;
        background: rgba(0, 229, 255, 0.15);
        color: #00e5ff;
        box-shadow: 0 0 20px rgba(0, 229, 255, 0.25);
      }
      .translate-hint {
        color: rgba(255, 255, 255, 0.25);
        font-size: 10px;
        font-family: "JetBrains Mono", monospace;
        letter-spacing: 1px;
        pointer-events: none;
        user-select: none;
      }

      /* 响应式 */
      @media (max-width: 1024px) {
        .hero-section {
          padding: 0 40px;
          flex-direction: column;
          justify-content: center;
          text-align: center;
        }
        .hero-content,
        .hero-visual {
          width: 100%;
        }
        .hero-visual {
          display: none;
        }
        h1 {
          font-size: 60px;
        }
        .description {
          margin: 0 auto 40px;
        }
        .btn-group {
          justify-content: center;
        }
        .section {
          padding: 100px 40px;
        }
        .features-grid {
          grid-template-columns: 1fr;
        }
        .stats-grid {
          grid-template-columns: 1fr;
          gap: 40px;
        }
        .translate-toggle {
          bottom: 30px;
          left: 20px;
        }
        .btn-translate {
          width: 48px;
          height: 48px;
          font-size: 11px;
        }
        .translate-hint {
          display: none;
        }
      }
    </style>
  </head>
  <body>
    <div id="canvas-container"></div>
    <div class="background-overlay"></div>

    <header class="site-header">
      <div class="nav-pill">
        <div class="logo">
          <div class="logo-icon"></div>
          <span>Aura</span>
        </div>
        <nav>
          <a href="#philosophy">Philosophy</a>
          <a href="#ecosystem" style="margin: 0 20px">Ecosystem</a>
          <a href="#">Rates</a>
        </nav>
      </div>
    </header>

    <div class="content-wrapper">
      <main class="hero-section">
        <div class="hero-content">
          <div class="status-tag">
            <div class="status-dot"></div>
            System Operational
          </div>
          <h1 class="gsap-hero-title">
            高级特效<br />
            <span style="display: block; line-height: 140%">小红书：王大黄黄黄博主.</span>
          </h1>
          <p class="description gsap-hero-desc">
           分享小兔子日常，赛博小兔期待您来，感谢关注！⭐️
          </p>
          <div class="btn-group gsap-hero-btns">
            <button id="contactMeBtn" class="btn-primary">获取微信</button>
          </div>
        </div>

        <div class="hero-visual gsap-hero-visual">
          <div class="svg-container">
            <div class="tech-label" style="top: 20%; left: 20%">
              Zero Latency
            </div>
            <div class="tech-label" style="bottom: 20%; right: 10%">
              Defi Native
            </div>
            <div class="radar-scanner"></div>
            <svg
              width="800"
              height="600"
              viewBox="0 0 800 600"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
            >
              <path
                d="M50 300C250 300 350 150 450 300C550 450 750 300 750 300"
                stroke="rgba(255,255,255,0.05)"
                stroke-width="1"
              />
            </svg>
          </div>
        </div>
      </main>

      <section id="philosophy" class="section">
        <div class="section-header gsap-fade-up">
          <span class="section-subtitle">// Core Philosophy</span>
          <h2 class="section-title">
            Architecting the<br />Future of <span>Liquidity</span>.
          </h2>
        </div>
        <div class="features-grid">
          <div class="feature-card gsap-card">
            <div class="feature-icon">🛡️</div>
            <h3 class="feature-title">Absolute Security</h3>
            <p class="feature-desc">
              Our smart contracts undergo rigorous institutional-grade auditing.
              Your assets are protected by state-of-the-art cryptographic
              safeguards.
            </p>
          </div>
          <div class="feature-card gsap-card">
            <div class="feature-icon">⚡</div>
            <h3 class="feature-title">Zero Latency</h3>
            <p class="feature-desc">
              Experience lightning-fast trade executions across L2 networks. We
              optimize every transaction to ensure minimal slippage.
            </p>
          </div>
          <div class="feature-card gsap-card">
            <div class="feature-icon">🧬</div>
            <h3 class="feature-title">Algorithmic Yield</h3>
            <p class="feature-desc">
              AI-driven auto-compounding vaults that dynamically rebalance your
              portfolio to maximize APY across multiple decentralized
              ecosystems.
            </p>
          </div>
        </div>
      </section>

      <section id="ecosystem" class="section stats-section">
        <div
          class="section-header text-center gsap-fade-up"
          style="text-align: center"
        >
          <span class="section-subtitle">// The Network Effect</span>
          <h2 class="section-title">Scale Without <span>Limits</span>.</h2>
        </div>
        <div class="stats-grid">
          <div class="stat-item gsap-stat">
            <h3 class="counter" data-target="2.4">0.0</h3>
            <p>Billion TVL (USD)</p>
          </div>
          <div class="stat-item gsap-stat">
            <h3 class="counter" data-target="150">0</h3>
            <p>Protocols Integrated</p>
          </div>
          <div class="stat-item gsap-stat">
            <h3 class="counter" data-target="99.9">0.0</h3>
            <p>Uptime Percentage</p>
          </div>
        </div>
      </section>

      <footer>
        <p>© 2024 Aura Protocol. All rights reserved. | Code by 王铁</p>
      </footer>
    </div>

    <div id="contactModal" class="cyber-modal-overlay">
      <div class="cyber-modal-content">
        <button class="cyber-close-btn" id="closeContactModal">&times;</button>
        <div class="modal-body">
          <p>关注小红书: 王大黄黄黄</p>
          <p class="highlight-text">输入以下文字获取: 赛博小兔一手讯息</p>
        </div>
      </div>
    </div>

    <div class="translate-toggle">
      <button class="btn-translate" id="translateBtn" title="翻译切换 (快捷键: T)">
        中
      </button>
      <span class="translate-hint">按 T 翻译</span>
    </div>

    <div class="floating-btn">
      <button class="btn-toggle" id="toggleBtn" title="打开效果控制面板">
        ⚙️
      </button>
      <div class="control-panel" id="controlPanel">
        <div class="panel-title">Effect Controls</div>
        <div class="control-group">
          <div class="control-label">
            <span>Edge Glow</span
            ><span class="control-value" id="edgeGlowValue">1.20</span>
          </div>
          <input
            type="range"
            id="edgeGlow"
            min="0"
            max="2"
            step="0.05"
            value="1.2"
          />
        </div>
        <div class="control-group">
          <div class="control-label">
            <span>Edge Width</span
            ><span class="control-value" id="edgeWidthValue">0.05</span>
          </div>
          <input
            type="range"
            id="edgeWidth"
            min="0.02"
            max="0.15"
            step="0.01"
            value="0.05"
          />
        </div>
        <div class="control-group">
          <div class="control-label">
            <span>Grid Resolution</span
            ><span class="control-value" id="gridResValue">140</span>
          </div>
          <input
            type="range"
            id="gridRes"
            min="20"
            max="150"
            step="5"
            value="140"
          />
        </div>
        <div class="control-group">
          <div class="control-label">
            <span>Voxel Size</span
            ><span class="control-value" id="voxelSizeValue">0.25</span>
          </div>
          <input
            type="range"
            id="voxelSize"
            min="0.1"
            max="0.6"
            step="0.05"
            value="0.25"
          />
        </div>
        <div class="control-group">
          <div class="control-label">
            <span>Glow Intensity</span
            ><span class="control-value" id="glowIntValue">1.20</span>
          </div>
          <input
            type="range"
            id="glowInt"
            min="0.5"
            max="3"
            step="0.1"
            value="1.2"
          />
        </div>
        <div class="control-group">
          <div class="control-label">
            <span>Cycle Duration</span
            ><span class="control-value" id="cycleValue">1.4</span>
          </div>
          <input
            type="range"
            id="cycle"
            min="0.5"
            max="10"
            step="0.1"
            value="1.4"
          />
        </div>
        <div class="control-group">
          <div class="control-label">
            <span>Distortion Strength</span
            ><span class="control-value" id="distortionValue">1.00</span>
          </div>
          <input
            type="range"
            id="distortion"
            min="0"
            max="3"
            step="0.1"
            value="1.0"
          />
        </div>
        <div class="control-group">
          <div class="control-label">
            <span>Blend Zone Width</span
            ><span class="control-value" id="blendZoneValue">0.15</span>
          </div>
          <input
            type="range"
            id="blendZone"
            min="0.02"
            max="0.4"
            step="0.02"
            value="0.02"
          />
        </div>
        <div class="divider"></div>
        <button class="reset-btn" id="resetBtn">Reset to Default</button>
      </div>
    </div>

    <script type="importmap">
      {
        "imports": {
          "three": "https://unpkg.com/three@0.160.0/build/three.module.js"
        }
      }
    </script>

    <script type="module">
      import * as THREE from "three";

      /* =========================================
         0. 翻译系统 (快捷键 T)
         ========================================= */
      let _lang = "en";
      const _dict = [
        ["Aura", "光环"],
        ["Philosophy", "理念"],
        ["Ecosystem", "生态系统"],
        ["Rates", "费率"],
        ["System Operational", "系统运行中"],
        ["Zero Latency", "零延迟"],
        ["Defi Native", "DeFi 原生"],
        ["// Core Philosophy", "// 核心理念"],
        ["Absolute Security", "绝对安全"],
        [
          "Our smart contracts undergo rigorous institutional-grade auditing. Your assets are protected by state-of-the-art cryptographic safeguards.",
          "我们的智能合约经过严格的机构级审计。您的资产由最先进的加密保护措施守护。",
        ],
        [
          "Experience lightning-fast trade executions across L2 networks. We optimize every transaction to ensure minimal slippage.",
          "体验跨 L2 网络的闪电般交易执行。我们优化每笔交易以确保最小滑点。",
        ],
        ["Algorithmic Yield", "算法收益"],
        [
          "AI-driven auto-compounding vaults that dynamically rebalance your portfolio to maximize APY across multiple decentralized ecosystems.",
          "AI 驱动的自动复利金库，动态再平衡您的投资组合，在多个去中心化生态系统中最大化年化收益率。",
        ],
        ["// The Network Effect", "// 网络效应"],
        ["Billion TVL (USD)", "十亿 TVL (美元)"],
        ["Protocols Integrated", "协议集成数"],
        ["Uptime Percentage", "正常运行时间百分比（%）"],
        ["Effect Controls", "效果控制"],
        ["Edge Glow", "边缘发光"],
        ["Edge Width", "边缘宽度"],
        ["Grid Resolution", "网格分辨率"],
        ["Voxel Size", "体素大小"],
        ["Glow Intensity", "发光强度"],
        ["Cycle Duration", "周期时长"],
        ["Distortion Strength", "扭曲强度"],
        ["Blend Zone Width", "混合区宽度"],
        ["Reset to Default", "恢复默认"],
        [
          "© 2024 Aura Protocol. All rights reserved. | Code by 王铁",
          "© 2024 Aura 协议。保留所有权利。 | 代码：王铁",
        ],
      ];
      // 按长度降序排列，确保长字符串优先匹配
      _dict.sort((a, b) => b[0].length - a[0].length);

      function _translateTextNodes(root, fromIdx, toIdx) {
        const walker = document.createTreeWalker(
          root,
          NodeFilter.SHOW_TEXT,
          { acceptNode: (n) => (n.parentElement.closest("script,style") ? NodeFilter.FILTER_REJECT : NodeFilter.FILTER_ACCEPT) },
          false,
        );
        const nodes = [];
        while (walker.nextNode()) nodes.push(walker.currentNode);
        for (const node of nodes) {
          for (const pair of _dict) {
            if (node.textContent.includes(pair[fromIdx])) {
              node.textContent = node.textContent.replace(pair[fromIdx], pair[toIdx]);
            }
          }
        }
      }

      // 处理跨元素文本（section titles 内部有 <br> 和 <span>）
      function _translateComplexElements(fromIdx, toIdx) {
        const sec1 = document.querySelector("#philosophy .section-title");
        if (sec1) {
          if (fromIdx === 0) {
            sec1.innerHTML = sec1.innerHTML
              .replace("Architecting the", "构建")
              .replace("Future of ", "未来")
              .replace("Liquidity", "流动性");
          } else {
            sec1.innerHTML = sec1.innerHTML
              .replace("构建", "Architecting the")
              .replace("未来", "Future of ")
              .replace("流动性", "Liquidity");
          }
        }
        const sec2 = document.querySelector("#ecosystem .section-title");
        if (sec2) {
          if (fromIdx === 0) {
            sec2.innerHTML = sec2.innerHTML
              .replace("Scale Without ", "无需")
              .replace("Limits", "限制");
          } else {
            sec2.innerHTML = sec2.innerHTML
              .replace("无需", "Scale Without ")
              .replace("限制", "Limits");
          }
        }
      }

      function toggleTranslation() {
        const btn = document.getElementById("translateBtn");
        if (_lang === "en") {
          _translateTextNodes(document.body, 0, 1);
          _translateComplexElements(0, 1);
          _lang = "zh";
          btn.textContent = "EN";
          btn.classList.add("active-lang");
        } else {
          _translateTextNodes(document.body, 1, 0);
          _translateComplexElements(1, 0);
          _lang = "en";
          btn.textContent = "中";
          btn.classList.remove("active-lang");
        }
      }

      document.getElementById("translateBtn").addEventListener("click", toggleTranslation);
      document.addEventListener("keydown", (e) => {
        // T 键翻译，忽略在输入框内时
        if (e.key === "t" || e.key === "T") {
          const tag = document.activeElement.tagName;
          if (tag === "INPUT" || tag === "TEXTAREA" || document.activeElement.isContentEditable) return;
          e.preventDefault();
          toggleTranslation();
        }
      });

      /* =========================================
         1. 弹窗交互逻辑 (新增)
         ========================================= */
      const contactBtn = document.getElementById("contactMeBtn");
      const contactModal = document.getElementById("contactModal");
      const closeContactBtn = document.getElementById("closeContactModal");

      // 打开弹窗
      contactBtn.addEventListener("click", (e) => {
        e.preventDefault();
        contactModal.classList.add("active");
      });

      // 关闭弹窗
      closeContactBtn.addEventListener("click", () => {
        contactModal.classList.remove("active");
      });

      // 点击背景遮罩关闭
      contactModal.addEventListener("click", (e) => {
        if (e.target === contactModal) {
          contactModal.classList.remove("active");
        }
      });

      /* =========================================
         2. 初始化 GSAP 动画
         ========================================= */
      gsap.registerPlugin(ScrollTrigger);

      const heroTl = gsap.timeline();
      heroTl
        .from(".status-tag", {
          y: 20,
          opacity: 0,
          duration: 1,
          ease: "power4.out",
          delay: 0.2,
        })
        .from(
          ".gsap-hero-title",
          { y: 50, opacity: 0, duration: 1.2, ease: "power4.out" },
          "-=0.8",
        )
        .from(
          ".gsap-hero-desc",
          { y: 30, opacity: 0, duration: 1, ease: "power3.out" },
          "-=0.9",
        )
        .from(
          ".gsap-hero-btns",
          { y: 20, opacity: 0, duration: 1, ease: "power3.out" },
          "-=0.8",
        )
        .from(
          ".gsap-hero-visual",
          { scale: 0.95, opacity: 0, duration: 1.5, ease: "expo.out" },
          "-=1.2",
        );

      gsap.to(".gsap-hero-visual", {
        y: -15,
        duration: 3,
        repeat: -1,
        yoyo: true,
        ease: "sine.inOut",
      });

      gsap.to(".hero-content", {
        scrollTrigger: {
          trigger: ".hero-section",
          start: "top top",
          end: "bottom top",
          scrub: 1,
        },
        y: 150,
        opacity: 0,
      });

      gsap.from(".gsap-fade-up", {
        scrollTrigger: {
          trigger: "#philosophy",
          start: "top 85%",
          toggleActions: "play none none reverse",
        },
        y: 50,
        opacity: 0,
        duration: 1.2,
        ease: "power4.out",
      });

      gsap.from(".gsap-card", {
        scrollTrigger: { trigger: ".features-grid", start: "top 85%" },
        y: 60,
        opacity: 0,
        duration: 1,
        stagger: 0.15,
        ease: "back.out(1.2)",
      });

      gsap.from(".gsap-stat", {
        scrollTrigger: {
          trigger: "#ecosystem",
          start: "top 80%",
          onEnter: () => animateCounters(),
        },
        y: 40,
        opacity: 0,
        duration: 1,
        stagger: 0.2,
        ease: "power3.out",
      });

      gsap.to(".nav-pill", {
        scrollTrigger: {
          trigger: "body",
          start: "50px top",
          end: "150px top",
          scrub: true,
        },
        backgroundColor: "rgba(5, 5, 5, 0.9)",
        boxShadow: "0 10px 40px rgba(0, 229, 255, 0.15)",
        padding: "10px 15px",
      });

      function animateCounters() {
        const counters = document.querySelectorAll(".counter");
        counters.forEach((counter) => {
          if (counter.classList.contains("animated")) return;
          counter.classList.add("animated");
          const target = +counter.getAttribute("data-target");
          const isFloat = target % 1 !== 0;
          gsap.to(counter, {
            innerHTML: target,
            duration: 2.5,
            snap: { innerHTML: isFloat ? 0.1 : 1 },
            ease: "expo.out",
            onUpdate: function () {
              if (isFloat) {
                counter.innerHTML = Number(this.targets()[0].innerHTML).toFixed(
                  1,
                );
              }
            },
          });
        });
      }

      let currentScrollY = window.scrollY;
      window.addEventListener(
        "scroll",
        () => {
          currentScrollY = window.scrollY;
        },
        { passive: true },
      );

      /* =========================================
         3. Three.js 背景着色器逻辑 
         ========================================= */
      const CONFIG = {
        edgeGlow: 1.2,
        edgeWidth: 0.05,
        gridRes: 140,
        voxelSize: 0.2,
        glowInt: 1.2,
        cycle: 1.4,
        distortion: 1.0,
        blendZone: 0.02,
      };
      const vertexShader = `varying vec2 vUv; void main() { vUv = uv; gl_Position = vec4(position, 1.0); }`;
      const fragmentShader = `
        uniform float uTime; uniform vec2 uResolution; uniform float uGridResolution; uniform float u_cycle; uniform float u_glow_intensity; uniform float u_voxel_size; uniform float u_edge_glow; uniform float u_edge_width; uniform float u_distortion; uniform float u_blend_zone; varying vec2 vUv;
        const vec3 color_bright = vec3(0.0, 0.8, 1.0); const vec3 color_dim = vec3(0.0, 0.1, 0.2);
        vec3 permute(vec3 x) { return mod(((x*34.0)+1.0)*x, 289.0); }
        float snoise(vec2 v){ const vec4 C = vec4(0.211324865405187, 0.366025403784439, -0.577350269189626, 0.024390243902439); vec2 i = floor(v + dot(v, C.yy) ); vec2 x0 = v - i + dot(i, C.xx); vec2 i1 = (x0.x > x0.y) ? vec2(1.0, 0.0) : vec2(0.0, 1.0); vec4 x12 = x0.xyxy + C.xxzz; x12.xy -= i1; i = mod(i, 289.0); vec3 p = permute( permute( i.y + vec3(0.0, i1.y, 1.0 )) + i.x + vec3(0.0, i1.x, 1.0 )); vec3 m = max(0.5 - vec3(dot(x0,x0), dot(x12.xy,x12.xy), dot(x12.zw,x12.zw)), 0.0); m = m*m ; m = m*m ; vec3 x = 2.0 * fract(p * C.www) - 1.0; vec3 h = abs(x) - 0.5; vec3 a0 = x - floor(x + 0.5); vec3 gr = a0 * vec3(dot(x0,x0), dot(x12.xy,x12.xy), dot(x12.zw,x12.zw)); m *= 1.79284291400159 - 0.85373472095314 * ( a0*a0 + h*h ); vec3 g; g.x = a0.x * x0.x + h.x * x0.y; g.yz = a0.yz * x12.xz + h.yz * x12.yw; return 130.0 * dot(m, g); }
        float voxelSDF(vec2 p, float size, float radius) { vec2 q = abs(p) - size + radius; return length(max(q, 0.0)) + min(max(q.x, q.y), 0.0) - radius; }
        void main() {
            vec2 aspect = vec2(uResolution.x / uResolution.y, 1.0); vec2 dist_to_corners = min(vUv, 1.0 - vUv) * aspect; float d_edge = min(dist_to_corners.x, dist_to_corners.y); vec2 center_uv = (vUv * 2.0 - 1.0) * aspect;
            float time = uTime * 0.3; float noise = snoise(center_uv * 1.5 + vec2(time * 0.2)); float noise2 = snoise(center_uv * 0.8 - vec2(time * 0.15)); float flow = (noise * 0.6 + noise2 * 0.4) * u_distortion * 0.1;
            vec2 distorted_uv = center_uv + center_uv * flow; float dist_from_center = length(distorted_uv); float max_radius = length(aspect) * 1.1; float progress = fract(time / u_cycle); float current_radius = progress * max_radius; float d_ring = abs(dist_from_center - current_radius);
            float edge_proximity = smoothstep(u_blend_zone, 0.0, d_edge); float wave_proximity = smoothstep(u_blend_zone, 0.0, d_ring); float dynamic_edge_width = u_edge_width * (1.0 + wave_proximity * 1.5); float edge_energy = exp(-d_edge / dynamic_edge_width) * u_edge_glow; edge_energy *= (0.9 + 0.1 * sin(uTime * 1.2)); edge_energy *= (1.0 + wave_proximity * 1.2);   
            float dynamic_wave_width = 0.01 + (edge_proximity * 0.04); float wave_energy = exp(-pow(d_ring, 2.0) / dynamic_wave_width) * u_glow_intensity; float lifecycle = smoothstep(0.0, 0.1, progress); wave_energy *= lifecycle;
            float final_brightness = wave_energy + edge_energy; final_brightness -= (wave_energy * edge_energy * 0.4); vec2 grid_uv = vUv * vec2(uGridResolution * aspect.x, uGridResolution); vec2 cell_local_uv = fract(grid_uv); vec2 voxel_local_uv = cell_local_uv - 0.5; float voxel_sdf = voxelSDF(voxel_local_uv, u_voxel_size, 0.05); float voxel_shape = 1.0 - smoothstep(-0.05, 0.05, voxel_sdf);
            float final_intensity = final_brightness * voxel_shape; vec3 final_color = mix(color_dim, color_bright, final_intensity); final_color *= final_intensity; gl_FragColor = vec4(final_color, 1.0);
        }
      `;
      const scene = new THREE.Scene();
      const camera = new THREE.OrthographicCamera(-1, 1, 1, -1, 0, 1);
      const renderer = new THREE.WebGLRenderer({
        antialias: true,
        alpha: true,
      });
      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
      renderer.setSize(window.innerWidth, window.innerHeight);
      document
        .getElementById("canvas-container")
        .appendChild(renderer.domElement);
      const geometry = new THREE.PlaneGeometry(2, 2);
      const uniforms = {
        uTime: { value: 0 },
        uResolution: {
          value: new THREE.Vector2(window.innerWidth, window.innerHeight),
        },
        uGridResolution: { value: CONFIG.gridRes },
        u_voxel_size: { value: CONFIG.voxelSize },
        u_cycle: { value: CONFIG.cycle },
        u_glow_intensity: { value: CONFIG.glowInt },
        u_edge_glow: { value: CONFIG.edgeGlow },
        u_edge_width: { value: CONFIG.edgeWidth },
        u_distortion: { value: CONFIG.distortion },
        u_blend_zone: { value: CONFIG.blendZone },
      };
      const material = new THREE.ShaderMaterial({
        uniforms,
        vertexShader,
        fragmentShader,
        transparent: true,
        blending: THREE.AdditiveBlending,
      });
      const mesh = new THREE.Mesh(geometry, material);
      scene.add(mesh);

      /* =========================================
         4. 右下角效果控制面板逻辑
         ========================================= */
      const toggleBtn = document.getElementById("toggleBtn");
      const controlPanel = document.getElementById("controlPanel");
      toggleBtn.addEventListener("click", () => {
        controlPanel.classList.toggle("open");
        toggleBtn.classList.toggle("active");
      });
      document.addEventListener("click", (e) => {
        if (!e.target.closest(".floating-btn")) {
          controlPanel.classList.remove("open");
          toggleBtn.classList.remove("active");
        }
      });

      const controls = {
        edgeGlow: document.getElementById("edgeGlow"),
        edgeWidth: document.getElementById("edgeWidth"),
        gridRes: document.getElementById("gridRes"),
        voxelSize: document.getElementById("voxelSize"),
        glowInt: document.getElementById("glowInt"),
        cycle: document.getElementById("cycle"),
        distortion: document.getElementById("distortion"),
        blendZone: document.getElementById("blendZone"),
        resetBtn: document.getElementById("resetBtn"),
      };
      const bindControl = (el, uniformKey, textId, isInt = false) => {
        el.addEventListener("input", (e) => {
          const val = parseFloat(e.target.value);
          uniforms[uniformKey].value = val;
          document.getElementById(textId).textContent = isInt
            ? Math.round(val)
            : val.toFixed(2);
        });
      };
      bindControl(controls.edgeGlow, "u_edge_glow", "edgeGlowValue");
      bindControl(controls.edgeWidth, "u_edge_width", "edgeWidthValue");
      bindControl(controls.gridRes, "uGridResolution", "gridResValue", true);
      bindControl(controls.voxelSize, "u_voxel_size", "voxelSizeValue");
      bindControl(controls.glowInt, "u_glow_intensity", "glowIntValue");
      bindControl(controls.cycle, "u_cycle", "cycleValue");
      bindControl(controls.distortion, "u_distortion", "distortionValue");
      bindControl(controls.blendZone, "u_blend_zone", "blendZoneValue");
      controls.resetBtn.addEventListener("click", () => {
        const updateVal = (el, key, confKey, textId, isInt = false) => {
          el.value = CONFIG[confKey];
          uniforms[key].value = CONFIG[confKey];
          document.getElementById(textId).textContent = isInt
            ? Math.round(CONFIG[confKey])
            : CONFIG[confKey].toFixed(2);
        };
        updateVal(
          controls.edgeGlow,
          "u_edge_glow",
          "edgeGlow",
          "edgeGlowValue",
        );
        updateVal(
          controls.edgeWidth,
          "u_edge_width",
          "edgeWidth",
          "edgeWidthValue",
        );
        updateVal(
          controls.gridRes,
          "uGridResolution",
          "gridRes",
          "gridResValue",
          true,
        );
        updateVal(
          controls.voxelSize,
          "u_voxel_size",
          "voxelSize",
          "voxelSizeValue",
        );
        updateVal(
          controls.glowInt,
          "u_glow_intensity",
          "glowInt",
          "glowIntValue",
        );
        updateVal(controls.cycle, "u_cycle", "cycle", "cycleValue");
        updateVal(
          controls.distortion,
          "u_distortion",
          "distortion",
          "distortionValue",
        );
        updateVal(
          controls.blendZone,
          "u_blend_zone",
          "blendZone",
          "blendZoneValue",
        );
      });

      window.addEventListener("resize", () => {
        renderer.setSize(window.innerWidth, window.innerHeight);
        uniforms.uResolution.value.set(window.innerWidth, window.innerHeight);
      });
      function animate(now) {
        const scrollOffset = currentScrollY * 0.0005;
        uniforms.uTime.value = now * 0.001 + scrollOffset;
        renderer.render(scene, camera);
        requestAnimationFrame(animate);
      }
      requestAnimationFrame(animate);
    </script>
  </body>
</html>
